# Fixture corpus

Generated by `tests/fixtures/make_fixture_corpus.py` -- **do not hand-edit**.
Regenerate with `uv run python tests/fixtures/make_fixture_corpus.py`;
`--check` fails if the current `ingest` would produce something else.

145 records, 3 provenance ranges.

## Whole pages

`get_page` has complete ground truth for these -- the fixture holds every
record the real corpus holds on them.

- `DccSUA` p.32 (21 records) -- records containing a `"` -- the ones a default csv.reader corrupts
- `DccSUA` p.75 (12 records) -- tables, captions, a footnote, dotted `B.n.n` cites
- `DccSUA` p.76 (22 records) -- the corpus's deepest ancestor chain (7), a formula, a second table
- `dog-habitabilidad` p.22 (20 records) -- the hand-verified 60º provision under A.2.2 < A.2; unaddressed `§` ids
- `dog-habitabilidad` p.25 (13 records) -- `m²` in body text -- the unit folding `norm` collapses to `m2`
- `dog-habitabilidad` p.32 (20 records) -- ids disambiguated with `~n`: the source prints one cite many times
- `DOG_2025` p.1 (21 records) -- a third document; picture records; the official `Pág. NNNNN` marker

## Partial pages

Pulled in only to complete an ancestor chain. `get_page` on these returns
less than the real corpus would; do not assert page completeness here.

- `DccSUA`: pp. 31, 74
- `dog-habitabilidad`: pp. 21, 23, 24, 31

## Shapes carried

- documents: 3
- chain depth >= 5: 27
- repeated cite in one doc: 13
- id disambiguated with ~: 11
- no cite: 87
- positional § id: 87
- text containing a ": 3
- table: 5
- caption: 7
- picture: 15
- formula: 1
- footnote: 1
- m² in text folded to m2 in norm: 2
- parent on an earlier page: 62
